version: 0.0
os: linux
files:
  - source: /
    destination: /home/ubuntu/django-app/
    overwrite: yes
permissions:
  - object:
hooks:
  AfterInstall:
    - location: scripts/deploy.sh
      timeout: 300
      runas: ubuntu

